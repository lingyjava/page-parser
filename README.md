# Page Parser - 网页解析Chrome插件

一个强大的Chrome浏览器插件，用于解析网页内容，支持为不同网站配置自定义的解析规则。

## 📁 项目结构

```md
page-parser/
├── packages/
│   ├── chrome/              # Chrome插件相关文件
│   │   ├── manifest.json    # 插件配置文件
│   │   ├── popup.html       # 弹出窗口HTML
│   │   ├── popup.css        # 弹出窗口样式
│   │   ├── popup.js         # 弹出窗口逻辑
│   │   ├── content.js       # 内容脚本
│   │   ├── background.js    # 后台服务Worker
│   │   ├── config.html      # 配置页面HTML
│   │   ├── config.css       # 配置页面样式
│   │   ├── config.js        # 配置页面逻辑
│   │   └── icons/           # 插件图标
│   └── core/                # 核心解析逻辑
│       ├── parser.js        # 解析器类
│       └── config.js        # 配置管理器
├── docs/                    # 文档目录
├── scripts/                 # 脚本目录
└── README.md               # 项目说明
```

## 🚀 安装使用

### 1. 加载插件

1. 打开Chrome浏览器，访问 `chrome://extensions/`
2. 开启右上角的"开发者模式"
3. 点击"加载已解压的扩展程序"
4. 选择 `packages/chrome` 目录

### 2. 配置解析规则

1. 点击插件图标打开popup
2. 点击"配置规则"按钮打开配置页面
3. 点击"添加新配置"
4. 输入网站域名（例如：www.example.com）
5. 添加解析字段和对应的CSS选择器
6. 保存配置

### 3. 使用插件

1. 访问已配置的网站
2. 点击插件图标
3. 查看解析预览
4. 选择"发送到接口"或"保存为JSON"

## 📝 配置说明

### CSS选择器语法

插件使用标准的CSS选择器来定位页面元素：

- **类选择器**：`.class-name`
- **ID选择器**：`#element-id`
- **标签选择器**：`div`, `p`, `h1` 等
- **属性选择器**：`[href]`, `[data-id]`
- **组合选择器**：`.parent > .child`, `.ancestor .descendant`

### 提取属性值

使用 `@属性名` 语法提取元素属性：

- `img@src` - 提取图片的src属性
- `a@href` - 提取链接的href属性
- `div@data-id` - 提取data-id属性
